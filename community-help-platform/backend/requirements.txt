# Core web framework
fastapi==0.111.0

# ASGI server — runs the FastAPI app
uvicorn[standard]==0.29.0

# HTTP utility library used internally by FastAPI
httpx==0.27.0

# Data validation (FastAPI depends on this)
pydantic==2.7.1

# Environment variable management (useful for secrets later)
python-dotenv==1.0.1

# ORM and database toolkit — maps Python classes to SQL tables
# SQLite driver is built into Python, so no extra driver package is needed.
sqlalchemy==2.0.30
