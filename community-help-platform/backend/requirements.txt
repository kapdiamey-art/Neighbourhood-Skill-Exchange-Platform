# Core web framework
fastapi>=0.111.0

# ASGI server — runs the FastAPI app
uvicorn[standard]>=0.29.0

# HTTP utility library used internally by FastAPI
httpx>=0.27.0

# Data validation — 2.10+ ships pre-built wheels for Python 3.14 (fixes the maturin/cargo error)
pydantic>=2.10.0

# Environment variable management
python-dotenv>=1.0.0

# ORM and database toolkit
sqlalchemy>=2.0.30

# Pure-Python MySQL driver — no Rust/C compilation needed (works with Python 3.14)
PyMySQL>=1.1.0

# Password hashing
bcrypt>=4.0.0
